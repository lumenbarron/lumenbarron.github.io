import React from 'react';
import './styles/know-me.css';

const DevLogo = props => (
    <section className={props.gral}>
    <section className={props.icon}/>
    <p className="icons-text">{props.iconText}</p>
  </section>
  );
  
  export default DevLogo